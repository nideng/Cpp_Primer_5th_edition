/*
A derived class is declared like any other class.
The declaration contains the class name but does not
include its derivation list.

so (a),(b),(c) are both incorrect;(a) derive from itself,
(b) and (c) are definition not declaration.
*/
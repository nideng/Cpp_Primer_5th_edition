/*
function template: Definition from which specific functions can be instantiated.

class template: Definition from which specific classes can be instantiated.

differ: the compiler cannot deduce the template parameter type(s) like function templates for a class template.
        we should supply the list of template arguments to use in place of the template parameters inside angle brackets 
        following the template's name.

*/
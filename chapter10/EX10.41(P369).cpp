/*
(a) replace the old_elements in the input range as new_elements.
(b) replace the elements in the input range which pred is true as new_elements.
(c) copy the new_elements which is old_elements in the input range into dest.
(d) copy the new_elements which pred is true in the input range into dest.
*/